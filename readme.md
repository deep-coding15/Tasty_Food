Pour les sessions, j'ai utilisé le design pattern "Singleton". 
Le design pattern "Singleton" est utile dans les cas suivants :
- une connexion à une base de données
- un gestionnaire de configuration
- une session utilisateur
- un logger


npx @tailwindcss/cli -i ./public/assets/css/style.css -o ./public/assets/css/output.css --watch